# Tests Makefile.am for NASFS

# Nothing to build in this directory, but list scripts for distribution
EXTRA_DIST = test_end_to_end.sh

# This directory has no programs to build
check_PROGRAMS =

# Clean up temporary test artifacts
clean-local:
	rm -rf workspace
